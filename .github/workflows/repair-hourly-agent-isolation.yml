name: Retired hourly autonomous-agent isolation repair

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-slim
    steps:
      - name: Retired
        run: exit 0
