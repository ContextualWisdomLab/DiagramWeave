name: Retired hourly proposal bundle RED verifier

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-slim
    steps:
      - name: Retired after RED evidence
        run: exit 0
