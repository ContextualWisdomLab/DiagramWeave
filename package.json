{
  "name": "@contextualwisdomlab/diagramweave",
  "version": "0.0.0",
  "private": true,
  "description": "Source-first, revision-safe AI editing foundation for PlantUML and future text diagram languages.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22 <25"
  },
  "scripts": {
    "test": "node --test",
    "coverage": "node scripts/run-coverage.mjs",
    "docstrings": "node scripts/check-docstrings.mjs",
    "syntax": "node scripts/check-syntax.mjs",
    "verify": "npm run syntax && npm run test && npm run coverage && npm run docstrings"
  },
  "devDependencies": {
    "c8": "12.0.0"
  }
}
